Test Plan for TeamTau FoodSorce

1)  Mapping UI


1.1	On startup, a map should appear with default location set to downtown vancouver
	as a marker.
		
	PASS

1.2 On startup, vendor markers should be presesnt on the map.

	PASS

1.3 On startup, if application is allowed to obatin user location, the user location 	 marker and focus will be set to that location.

	PASS

1.4 On startup, the distance option will be set at "all".

	PASS

1.5 User should be able to interact with the map by zooming and moving around.

	PASS

1.6 User can input own address or location through text box, and send request 	
    the result by pressing enter or hitting the set location button.

    PASS

1.7 When user sets current location, the user location 	 marker and focus will
	be set to that location.

	PASS

1.8 When user sets current location to be either an invalid location or a 
    location outside of vancouver, an error message will appear requesting another address within Vancouver.

  	PASS

1.9 When new location is set, distance option does not change.

	PASS

1.10 When new distance option is set, distance does not change

	 PASS

1.11 When new distance is set, markers appear/dissapear wih accordance to the 
	 distance option selected.

	 PASS

1.12 Selecting an already selected distance option does not do anything.

	 PASS

1.13 Default field for address field is "address" and text is highlighted when selected

	 PASS

1.14 Selecting Main Page, Profile, Admin or Sign Out does not effect map.

	 PASS


2) General UI


2.1 On startup, the Title, "FoodSorce" and a menu on the left should be present

	PASS

2.2 On startup, left of the map a log in page should be shown with a link to login 		with your Google Account.

	PASS

2.3 On startup, the menu on the left should contain Main Page, Profile, Admin and Sign 		Out links.

	PASS

2.4 Clicking Main Page should link to the vendor list. 

	PASS

2.5 Clicking Main more than once does not do anything.

	PASS

2.6 Clicking Profile should link to the not yet completed profile page. 

	PASS

2.7 Clicking Profile more than once does not do anything.

	PASS

2.8 Clicking Admin should link to the Administrator Console page. 

	PASS

2.9 Clicking Admin more than once does not do anything.

	PASS

2.10 Clicking Sign Out should log the user out, if user is signed in, and refresh the 	   page to the login page.

	PASS

2.11 Clicking Sign Out more than once or when not signed in does not do anything 		 except refresh the page

	PASS

2.12 Administrator Console Page should display a clickable button labled "Import Data"

	PASS

2.13 Clicking import data should display a pop-up displaying whether import was 		successful or failed.

	PASS

2.14 Clikcing import data mutliple times does not crash program.

	PASS

3) Vendor Display UI

3.1 Vendor display displays all venues on startup

	PASS

3.2 Vendor display changes corresponding to the correct distance option selected.
	
	PASS

3.3 Vendor Name, Type of Food, Quality, Cost and Location are all correctly displayed.

	PASS

3.4 User is able to scroll through the vendor list

	PASS







